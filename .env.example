# ─── NextAuth v5 (Google provider) ───────────────────────────────────────────
# 1. Go to https://console.cloud.google.com → APIs & Services → Credentials
# 2. Create an OAuth 2.0 Client ID (Web application)
# 3. Authorized redirect URIs: http://localhost:3000/api/auth/callback/google
#    (add your production domain too, e.g. https://yourdomain.com/api/auth/callback/google)
# 4. Generate secret: openssl rand -hex 32
# NOTE: NextAuth v5 uses AUTH_SECRET / AUTH_URL (not NEXTAUTH_SECRET / NEXTAUTH_URL)
AUTH_SECRET='use [openssl rand -hex 32] to generate a 32 bytes value'
AUTH_URL='http://localhost:3000'
AUTH_TRUST_HOST='true'
GOOGLE_CLIENT_ID='YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com'
GOOGLE_CLIENT_SECRET='GOCSPX-YOUR_GOOGLE_CLIENT_SECRET'

# ─── Neon DB (PostgreSQL) ─────────────────────────────────────────────────────
# Found at: https://console.neon.tech → Your project → Connection Details
# Use the "Pooled connection" string (ep-...-pooler) for serverless/edge functions.
DATABASE_URL='postgresql://neondb_owner:YOURPASSWORD@ep-xxx-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'
# ─── Voting security ─────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
HASH_PEPPER='your_random_pepper_here'

# ─── Geo restriction ─────────────────────────────────────────────────────────
# Set to 'true' to enforce Antioquia-only geo restriction
GEO_RESTRICT='true'
